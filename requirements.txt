# Install CUDA wheel matching the Spark host for CORTEX GPU runs.
# Exact torch/cuda/cudnn/device strings are pinned in docs/cortex_birth.lock.
torch>=2.0
numpy>=1.26
matplotlib>=3.8
cvxpy==1.7.3
clarabel==0.11.1
scipy==1.18.0
