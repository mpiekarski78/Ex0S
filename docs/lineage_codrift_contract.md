# TM.0.61.CODRIFT contract

**Lab:** TM.0.61.CODRIFT · **Not a v41 candidate.** Product **0.0.004**. Diagnostic only. No neural edit. No SOCP installation. Do not install W\*. Do not redesign K/Q/V. Do not retune `EPISODE_MATCH_L2` (0.05). Do not rerun or edit TM060.

TM060 first-match `representation_drift` stays frozen. Architectural conclusion remains none. TM060 earned this localization wall, not an architecture. Fast Weight Programmers / Miconi is the research family, not yet a mechanism.

## Matrix

One chronological split of receipt-identified attempted write-time \(v_t\). Ordinary reference constraints retained. Every \(W^*\) discarded.

| Decoder | Prefix values | Later values |
| --- | --- | --- |
| Prefix-time \(W^*\) | baseline | TM060 failure |
| Later-time \(W^*\) | test | test |

Then one shared linear map and one shared orthogonal (Procrustes) map, used only to score already-held-out values. They are not SOCP constraints and are not installed.

## Ladder (setup excluded)

`setup_precondition_fail` → `observer_used_runner_provenance` → `full_oracle_infeasible` → `prefix_baseline_fail` → `tm060_cross_not_reproduced` → `later_values_unreadable` → `transport_restores_decoding` → `nonlinear_manifold_reorganization` → `matching_time_co_drift`

| Result | Interpretation |
| --- | --- |
| Later \(W^*\) cannot decode later values | Value formation remains unreadable; plastic representation learning is the earned target |
| One shared linear/orthogonal transport restores decoding | Generic coordinate alignment may suffice |
| No shared transport works, full oracle remains feasible | The manifold is reorganizing nonlinearly; evolved/plastic representation formation is the larger jump |
| Only matching-time pairs pass | Representation and motor decoder co-drift; need a stable memory coordinate or learned transport, not stronger storage |

## Refuse

Neural edit, SOCP install, install W\*, K/Q/V redesign, predict organism ids, retune 0.05, rewrite TM060, v41, product-earn.
