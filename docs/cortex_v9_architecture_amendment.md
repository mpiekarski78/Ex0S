# CORTEX v9 architecture amendment

Authorized only by [`cortex_diagnosis.v8.lock`](cortex_diagnosis.v8.lock).

Retain v7/v8 neural. D1 bind/teach/probe uses only the press and harm handles so get/drop cannot capture body-advantage credit during the D1 test. D2 may bind the full motor set afterward. No new credit law.
