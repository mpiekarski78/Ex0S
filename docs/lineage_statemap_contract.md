# Lineage STATEMAP contract — TM.0.24.STATEMAP

**Lab:** TM.0.24.STATEMAP  
**Subtitle:** Developmental state transfer and maturation  
**Product:** Ex0S **0.0.004**  
**Flags:** `earned_next=false`, `ex0s=null`, `eligible_for_000005=false`  
**Live candidate:** v29 (immutable this package)

Not a lineage rescore. Not a capability earn. Not a neural amendment. n stays **64**.

Authorized by [`lineage_actorcredit_reach.lock`](lineage_actorcredit_reach.lock): A0–A11 proved complete motor-credit, then ordinary development only moved CHECK from 0.12 to 0.16. The actor can learn in controlled cells; the learned change does not reliably transfer through a complete developmental life.

Working hypothesis (not yet the diagnosis): the recurrent state that receives credit during teaching is not sufficiently aligned with the state present during later autonomous probes.

## Frozen cells (unused worlds)

| Cell | Question |
| --- | --- |
| S0 | Does one beneficial ACT change an immediate identical-state probe? |
| S1 | Does it survive EVENT_END → EVENT_START with the same cue? |
| S2 | Does it survive delays of 1/2/4/8 neutral ticks? |
| S3 | Does it survive increasing irrelevant-event interference? |
| S4 | Is the acquired behavior reconstructed from cortex/S after `ρ` reset? |
| S5 | Does the same observable cue work after a different preceding history? |
| S6 | Can two cues acquire opposite actuator consequences without collision? |
| S7 | Does the credited teaching state align with the later autonomous probe state? |
| S8 | If motor preference exists, does the operation state still suppress ACT? |
| S9 | Compare immediate fast policy, post-REST policy, and slow weights |
| S10 | Do age schedules actually change effective learning/exploration parameters? |
| S11 | Does perturbing each declared Arm D gene cause a measurable runtime effect? |
| S12 | Do the same state-transfer conclusions survive independent renaming? |

S7 must measure more than raw cosine: teaching-vs-probe similarity; contribution of each state to the `W_op` ACT logit and the beneficial handle score; behavior after substituting the saved teaching state at probe; behavior after projecting the probe state onto the teaching state.

## Decision table

- Immediate identical-state fails → plasticity magnitude, normalization, or update/readout mismatch.
- Immediate passes; event boundary fails → event-state persistence / boundary architecture.
- Boundary passes; distractors fail → interference and state stabilization.
- Saved teaching state succeeds at probe, live probe state fails → representation alignment.
- State and motor ranking succeed but ACT fails → operation/HOLD competition outside the controlled cell.
- Fast policy works but post-REST fails → consolidation, despite D4’s bounded pass.
- Age/development genes have no measurable effect → dead developmental genome surface.
- Age genes act but reduce learning → maturation schedule/dynamics.
- All deterministic state cells pass but complete life fails → curriculum interaction or unresolved stochasticity.

## After the cells

Choose an architecture amendment **only** from this decision table. A two-timescale state inside the existing 64 units remains a hypothesis until S7 interventions demonstrate alignment failure. Do not increase n. Do not add an L0 function.

## Refuse

Neural edit this package; L0-specific circuitry; QUAL/EVAL reveal; rewriting LINEAGE/WALLMAP/REACH/PLASTICITYMAP/ACTORCREDIT/v28/v29 locks; `earned_next`; 0.0.005; moving `τ`/`δ`; increasing n; FULLDEV.R7; another lineage run; Q3; treating clamp as a teaching oracle.
