# CORTEX isolated D6.R1 statistical contract

**Lab:** TM.0.23.CORTEX.D6.R1.STAT  
**Product:** 0.0.004 · `earned_next=false` · `ex0s=null`  
**Authorized by:** [`cortex_diagnosis.fulldev_r4.lock`](cortex_diagnosis.fulldev_r4.lock)

Narrow D6 only. Historical `score_d6` unchanged. Do not lower `emits>=2`, `matched>=1`, or `holds>=3`.

Worlds from a new sealed commitment, domain `TM023.D6.R1.`.

Each isolated life runs D0, binds, light `[a,b]` teach, historical `score_d1`–`score_d5`, then `score_d6`. Pair clear requires D0 and D6 only.

Authorized neural law (implement only after this apparatus is on `origin/main`):

1. Keep v16–v20 HOLD / habituation / S-write / RETRIEVE recency laws.
2. **Echoic emit:** maintain a decaying buffer of recently observed symbols. When the motor chooses EMIT, prefer those echoic symbols over an ungrounded `W_emit_query` argmax.

Gate clear ≥13/16 pairs. Re-earn C4/C5/C6 before reveal.
