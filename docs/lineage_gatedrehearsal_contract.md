# Prediction-error-gated competitive rehearsal — TM.0.27.GATEDREHEARSAL

**Lab:** TM.0.27.GATEDREHEARSAL

Not a product earn. Product **0.0.004**. `earned_next=false`. `ex0s=null`.

Authorized by [`cortex_v34.prereg.lock`](cortex_v34.prereg.lock) after [`lineage_competitive.decision.addendum.lock`](lineage_competitive.decision.addendum.lock). v33 competitive evidence (`competitive_core_acquire_fail`, 7/8 eight-cue) remains historical. This package scores **prediction-error-gated competitive rehearsal** on unused worlds.

## Law

See [`cortex_v34_architecture_amendment.md`](cortex_v34_architecture_amendment.md). Preserve v33 competitive geometry and eight-slot store. Replace uniform REST replay-all with violation-gated rehearsal during awake (after each episode write) and REST. One slow β mix after REST gated block. Measure consolidation separately.

## Battery

Unused `TM027.GATEDREHEARSAL.DEV.` / `TWIN.`. SCORE reserved. **54 cells** with frozen manifest hash (lab + domains + cell IDs). Pass statistic is normalized geometric margin ≥ 0.01 plus unique ranking and perturbation on stable cells.

## Diagnostics (runner-owned aggregation)

Per credit event: up to 16 awake rehearsal passes. Per REST: 16 gated epochs then slow mix.

**Consolidation checkpoints:** `stored_post_awake`, `stored_post_rest_rehearsal_pre_mix`, `stored_post_slow_mix`, `live_probe`.

**Failure classes:** `store_nonconvergence`, `consolidation_margin_loss`, `reinstatement_wall`, `store_and_live_fail`, `none`.

All diagnostic floats must be finite. Reject NaN/Inf.

## Refuse

Unconditional replay-all; low-margin awake alone; unbounded organism diagnostic memory; auto `cortex.candidate.v34.lock`; rewrite TM026/v33 historical locks; SCORE; `earned_next`; 0.0.005; runner edits after Phase 1 push through DEV.
