# Two-timescale P1 consolidation — TM.0.25.TWOSCALE

**Lab:** TM.0.25.TWOSCALE

Not a lineage rescore. Not a capability earn. Not another solver MAP. Product **0.0.004**. `earned_next=false`. `ex0s=null`.

Authorized by AFFINEMAP R2 **online_optimization_failure**. A1 passed; A2 failed four-cue acquire ranking; A3 did not uniquely help. Install one two-timescale candidate in the organism and score acquire / stability / reversal / specificity on unused worlds.

## Law

See [`cortex_v32_architecture_amendment.md`](cortex_v32_architecture_amendment.md). Fast episodic P1 memory (8 slots, content-addressed replacement) plus error-driven awake updates plus REST replay / low-margin strengthening plus slow `W_act_query` consolidation. n=64. Zero innate cue-action maps. Default scoring stays query. W1 stays off. A3 bias stays out.

## Battery

Unused `TM025.TWOSCALE.DEV.` / `TWIN.`. SCORE reserved. Pass statistic is normalized geometric margin on effective `W_act_query` rows at P1, gate 0.01, plus unique ranking and perturbation. Acquire pass is ranking. Stability pass is ranking ∧ γ≥0.01 ∧ perturbation after REST.

## Refuse

Another MAP; v31 W1; A3; compact PA as the path; oracle reaudit; 512-row TRACE; 1536 eligibility; SCORE; `earned_next`; 0.0.005; rewrite historical locks.
