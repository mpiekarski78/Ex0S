# Affine vs homogeneous acquisition — TM.0.24.AFFINEMAP

**Lab:** TM.0.24.AFFINEMAP  
**Product:** Ex0S **0.0.004**  
**Flags:** `earned_next=false`, `ex0s=null`, `eligible_for_000005=false`  
**Live candidate:** v30, default v29 query scoring.  
**n:** **64**

Not a lineage rescore. Not a capability earn. **Not a neural amendment.** No trace install. No `cortex.candidate.v31.lock`. Do not resurrect W1. Do not open SCORE. Do not test another isolated write formula. Do not rewrite LIFECYCLEMARGINMAP, MEMORYLIFECYCLEMAP R2, or V1. Do not investigate consolidation.

Authorized by [`lineage_lifecyclemarginmap.decision.lock`](lineage_lifecyclemarginmap.decision.lock) and [`lineage_lifecyclemarginmap.decision.addendum.lock`](lineage_lifecyclemarginmap.decision.addendum.lock). Historical LIFECYCLEMARGINMAP first-match **consolidation_margin_loss** is preserved and interpreted as **consolidation_margin_loss_under_min_unique_winner_margin_without_ranking_ok**. Honest first-match on those cells is **lifecycle_margin_insufficient**.

## How to read the LIFECYCLEMARGINMAP result

The addendum is correct. The first real failure is M1 four-cue acquire ranking, before REST or lifecycle stability can be evaluated. Consolidation is not the next question.

M1/C3 passive-aggressive scores \(w^\top x/\|w\|\) with **b=0**. M3/D1 scores \(w^\top x+b\). If D1's eight-cue success depends on a nonzero intercept, the oracle and the online learner are solving different problems. PA may be restricted to a decision boundary through the origin rather than failing optimization.

The ecological σ=0.01 content matcher remains a separate radius limitation. It is not this diagnosis. The immediate wall is online acquisition geometry.

## Extract first (not a scored DEV)

Frozen M3 cells do not store \(w\) or \(b\). Reconstruct the frozen M3 capture path on unused `TM024.LIFECYCLEMARGINMAP.DEV.` **without writing that DEV lock**. For every 2/4/8-cue acquire cell report:

1. D1 fitted \(b\)
2. Geometric and ranking margin with fitted \(b\)
3. The same separator after setting \(b=0\)
4. Exact optimal homogeneous hard-margin constrained to \(b=0\)

Do not edit [`experiments/run_tm024discrimmap_r2.py`](../experiments/run_tm024discrimmap_r2.py). The homogeneous solver lives in this package.

## Question

On the same L2 P1 store, cue counts, twins, and perturbations, with **no lifecycle changes** (no eco/spec, no live reversal cells), does four/eight-cue acquire ranking require an affine intercept, or does a homogeneous separator exist that frozen PA fails to reach?

No new η grid, match radius, replay budget, or threshold.

## Arms (frozen)

Runner-only. Exact P1 bridge. Unused `TM024.AFFINEMAP.DEV.` / `TWIN.`. SCORE reserved. A3 is **diagnostic only** and **not authorized**.

| Arm | Class |
| --- | --- |
| A0 | Existing affine D1, \(b\) free |
| A1 | Exact hard-margin D1, \(b=0\) |
| A2 | Existing PA, \(b=0\) |
| A3 | PA with learned actuator-local bias, diagnostic only |

A0/A1 are one batch fit on valid stored rows. A2 is the frozen C3 PA replay. A3 keeps that PA weight step and adds per-handle bias scalars, initialized at 0, updated only from experienced consequences, permuted with opaque actuator slots, with no cue strings or semantic action identities. A full bank would be eight fast scalars, perhaps eight slow copies. This grid uses two handles. **A3 is not an instinct and is not authorized.**

104 cells: 4 arms × (2/4/8 × 2 worlds × 2 orders × acquire/stable + 2 twins). Acquire is ranking only. Stability still reports native margin 0.01 and ranking perturbation σ=0.01 after REST. The ladder uses acquire ranking at four and eight cues.

## Decision ladder (disjoint, frozen order)

1. A0 passes acquire ranking at four and eight cues, and A1 fails at four or eight → `affine_intercept_required`
2. A1 passes acquire ranking at four and eight cues, and A2 fails at four or eight → `online_optimization_failure`
3. A2 passes acquire ranking at four and eight cues while M1 previously failed four-cue acquire ranking → `apparatus_inconsistency`
4. A3 passes acquire ranking at four and eight cues, and A2 fails at four or eight → `learned_local_bias_supported`
5. A0 and A1 both fail acquire ranking at four cues, or both fail at eight → `d1_ceiling_reaudit`
6. Else → `affine_map_insufficient`

Rung 4 does not authorize a neural amendment.

## Refuse

Trace install; v31/v32; W1; neural edit; D5; opening SCORE; installing 512/1536; larger n; lineage; QUAL/EVAL; FULLDEV.R7; rewriting historical locks including LIFECYCLEMARGINMAP / MEMORYLIFECYCLEMAP R2/V1; `earned_next`; 0.0.005; instincts; authorizing A3; SFNN; another isolated write-formula grid; cue strings in episodes; new η / radius / replay budget / threshold; lifecycle changes; eco/spec cells; investigating consolidation; using bounded match sanity as the stability gate; treating the global σ=0.01 matcher as this package's first failure.
