# TM.0.24.ELIGMAP DEV

Decision: **trace_separates_linear_fails**. Next: `competitive_or_discriminative_write_geometry`.

Eλ does not robustly pass at eight cues. E0/E1/EΔ also fail the frozen 0.01 margin. At two cues, N0 ranking is correct on E0 (margin ~3e-4) and E1 (margin ~2e-3); both remain crumbs. At eight cues Eλ is pairwise-distinct under COLLISIONMAP L2/cosine thresholds, but N0 scores 4/8 (last-write-wins on a two-handle set). N1/N2 repair ecological reversal on E0, E1, and Eλ@0.9; N0 does not. That secondary finding is recorded; the first-match ladder is competitive/discriminative write geometry, not an eligibility install.

No neural edit. SCORE unopened. No `cortex.candidate.v31.lock` / `v32`. Product **0.0.004**. `earned_next=false`.
