# Trace-sufficiency bridge — TM.0.24.TRACEBRIDGE

**Lab:** TM.0.24.TRACEBRIDGE  
**Product:** Ex0S **0.0.004**  
**Flags:** `earned_next=false`, `ex0s=null`, `eligible_for_000005=false`  
**Live candidate:** v30, default v29 query scoring.  
**n:** **64**

Not a lineage rescore. Not a capability earn. **Not a neural amendment.** No `cortex.candidate.v31.lock`. Do not resurrect W1. Do not open SCORE. The 512-scalar event-end trace and the 1,536-scalar ceiling stay **unopened**.

Authorized by [`lineage_phasemap.decision.lock`](lineage_phasemap.decision.lock) and [`lineage_phasemap.decision.addendum.lock`](lineage_phasemap.decision.addendum.lock). PHASEMAP first-match **state_reinstatement_instability**: P0/P1 are robust at eight cues; first fail is P2 (teaching interpolates, live probes do not transfer). Last robust causal source is **P1** (event-end / innate `v_end` tick).

## Question

If the last robust P1 state is causally carried to the action and credit ticks, does the unchanged v29 learning law work?

## Arms (frozen)

| Arm | Address | Learner |
| --- | --- | --- |
| B0 | Current P5 (`ρ_elig`) | Unchanged v29 `W_act_query` outer product |
| B1 | Exact P1 bridge | Unchanged v29 ACT scoring and credit |
| B2 | Event-boundary local trace | Unchanged v29 |
| B3 | P1 bridge | Frozen D3 competitive rule, diagnostic only |
| B4 | P1 | Frozen D1 hard-margin oracle ceiling |

B1 is the ideal causal upper bound. The same P1 vector is supplied to live ACT scoring and later credit. B2 is the installable object: eight actuator-local 64-d rows, updated only at the innate `v_end` injection (structural event-end), frozen through observable and motor ticks, λ=0 (last event-end register). It does not use cue identity or semantic content. B0 is the collapsed-address control. B3/B4 do not change the v29 write law in the organism.

## Battery (frozen)

Balanced 2/4/8-cue maps, both teaching orders, renamed twins, frozen perturbations (`σ=0.01`, `n=20`, `≥19/20`), delayed select→credit, positive and negative outcomes, ecological reversal, HOLD and REST controls. Native ranking margin 0.01 for B0–B3. D1 geometric margin 0.01 for B4. Fit teaching addresses only.

## Decision ladder (disjoint, frozen order)

1. B4 fails at eight cues → `revisit_phasemap_apparatus`
2. B1 trains but twins/perturbations fail → `p1_transport_interpolation_only`
3. B1 and B2 pass with v29 → `authorize_trace_only_keep_v29_write`
4. B1 passes, B2 fails → `p1_sufficient_trace_dynamics_fail`
5. B1 fails, B3 passes → `transport_and_discriminative_write_required`
6. B1 and B3 fail → `p1_not_usable_by_online_class`

A later freeze may install the 512-row event-end trace only if this package returns `authorize_trace_only_keep_v29_write`. This package does not edit `NeuralCortex`.

## Refuse

v31/v32; W1; neural edit; D5; opening SCORE; installing 512 or 1,536; larger n; lineage; QUAL/EVAL; FULLDEV.R7; rewriting historical locks; `earned_next`; 0.0.005; instincts; SFNN.
