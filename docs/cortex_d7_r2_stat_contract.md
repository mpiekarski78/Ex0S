# CORTEX isolated D7.R2 statistical contract

**Lab:** TM.0.23.CORTEX.D7.R2.STAT  
**Product:** 0.0.004 · `earned_next=false` · `ex0s=null`  
**Authorized by:** [`cortex_diagnosis.v24.lock`](cortex_diagnosis.v24.lock)

Narrow D7 only. Historical `score_d7` unchanged. Do not lower `distinct_lengths>=2`, `max_length>=4`, `max_length<=8`, `terminals>=15`, or `curriculum_hits>=2`.

Worlds from a new sealed commitment, domain `TM023.D7.R2.`.

Each isolated life runs D0, binds, light `[a,b]` teach, historical `score_d1`–`score_d6`, then `score_d7`. Pair clear requires D0 and D7 only.

Authorized neural law (implement only after this apparatus is on `origin/main`):

1. Keep v16–v23 laws, including echoic emit and inter-observe vocal refractory.
2. Replace softmax utterance persist with hard phrase unroll + phrase-final STOP.

Gate clear ≥13/16 pairs. Re-earn C4/C5/C6 before reveal.
