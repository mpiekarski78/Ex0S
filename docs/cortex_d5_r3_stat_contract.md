# CORTEX isolated D5.R3 statistical contract

**Lab:** TM.0.23.CORTEX.D5.R3.STAT  
**Product:** 0.0.004 · `earned_next=false` · `ex0s=null`  
**Authorized by:** [`cortex_diagnosis.fulldev_r6.lock`](cortex_diagnosis.fulldev_r6.lock)

Narrow D5 only. Historical `score_d5` unchanged. Do not lower `unknown_holds>=12`, `known_nonhold_rate>=0.30`, or the `+0.15` margin.

Worlds from a new sealed commitment, domain `TM023.D5.R3.`.

Each isolated life runs D0, binds, light `[a,b]` teach, historical `score_d1`–`score_d4`, then `score_d5`. Pair clear requires D0 and D5 only.

Authorized neural law (implement only after this apparatus is on `origin/main`):

1. Keep v16–v25 laws, including phrase-final STOP for familiar phrases.
2. Do not unroll a phrase when any current-phrase symbol has `fam < FAMILIARITY_ABS`; HOLD instead.

Gate clear ≥13/16 pairs. Re-earn C4/C5/C6 before reveal.
