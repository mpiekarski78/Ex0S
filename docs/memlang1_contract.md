# MEMLANG-1 contract

**Program:** MEMLANG-1 · **Not TM063.** **Not a v41 candidate.** Product **0.0.004**.

TM062 closed the transport branch. A decoder exists for combined write-time states, but finite earlier experience does not determine an action-invariant coordinate. MEMLANG-1 searches representation learning through repeated developmental lives. It does not guess one rule per week.

Live pointer stays [`docs/cortex.candidate.lock`](cortex.candidate.lock) v30. Never write `cortex.candidate.v41.lock`. Do not open `experiments/run_tm063*.py` as a diagnostic wall.

## Law

Canonical write-time law stays frozen: \(k_t = W_k\,\rho_{\mathrm{cue},t}\), \(v_t = \mathrm{unit}(\rho_{\mathrm{post\text{-}feedback},t})\). Values are generated online at event time. The runner never constructs the stored value and never copies an answer handle into S. Receipt identity follows TM059. Teaching signal is organism-observed motor feedback and reward.

Existing `MEMPROJ_LEARNED` is not a Stage A family: it jointly updates K/Q/V and constructs provenance ids.

## Stages

A invariant value formation → B memory necessity → C organism-owned addressing → D micro-language life → E evolution.

Only Stage A is executable at freeze. B–E are locked until the previous integrated gate is green.

## Loop

Internal failures are telemetry under `runs/memlang1/`, not lineage releases.

propose → newborn lives → classify failure → inspect geometry → mutate/combine → rerun → retain or kill.

Only a selected mechanism may receive a frozen candidate commit and one untouched confirmation battery.

## Refuse

Install W\*, retune 0.05, alter opaque-store law, redesign K/Q/V in Stage A, TM063 diagnostic, auto v41, product-earn, treat TM061/TM062 as an architecture.
