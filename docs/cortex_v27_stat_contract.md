# CORTEX isolated v27 statistical contract

**Lab:** TM.0.23.CORTEX.V27.STAT  
**Product:** 0.0.004 · `earned_next=false` · `ex0s=null`  
**Authorized by:** [`cortex_diagnosis.v26_generality.lock`](cortex_diagnosis.v26_generality.lock)

n_pairs = 16. gate_clear_min_pairs = 13. Domain `TM023.V27.GEN.`.

G3 and G5 pass rules are identical to [`cortex_v26_generality_contract.md`](cortex_v26_generality_contract.md). Do not soften them after seeing v26 reds.

G1 uses the same frozen forbidden identifier/regex list as GENERALITY.v26.

Worlds from a new sealed 256-bit eval seed. Reveal only from a clean tree pinning `git rev-parse HEAD` after candidate.v27 is on `origin/main`.
