# CORTEX isolated D5.R2 statistical contract

**Lab:** TM.0.23.CORTEX.D5.R2.STAT  
**Product:** 0.0.004 · `earned_next=false` · `ex0s=null`  
**Authorized by:** [`cortex_diagnosis.v19.lock`](cortex_diagnosis.v19.lock)

Narrow D5 only. Historical `score_d5` unchanged. Do not lower `unknown_holds>=12`.

Worlds from a new sealed commitment, domain `TM023.D5.R2.`.

Each isolated life runs D0, binds, light `[a,b]` teach, historical `score_d1`–`score_d4`, then `score_d5`. Pair clear requires D0 and D5 only.

Authorized neural law (implement only after this apparatus is on `origin/main`):

1. Keep v19 decaying familiarity trace.
2. Raise `FAMILIARITY_ABS` from 12 to 16 so novelty HOLD covers more of the 20 unknown probes.
3. Keep `FAMILIARITY_DECAY=0.98`, equal-evidence, S-write recency, RETRIEVE recency tie-break.

Gate clear ≥13/16 pairs. Re-earn C4/C5/C6 before reveal.
