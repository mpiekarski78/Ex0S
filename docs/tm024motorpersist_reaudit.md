# TM.0.24.MOTORPERSIST equal-advantage re-audit

24 DEV sequences (`p` grid × 2 worlds × both orders). Both handles share one positive MID delta. A→B both select B: **True**. B→A both select A: **True**. Opposing any: **False**. Teaching-state cosine [0.997789, 0.999662]; L2 [0.075024, 0.236467].

Diagnosis unchanged: **identity_survives_opposing_learning_fails**. SCORE unopened. Historical MOTORPERSIST locks untouched. Next is plastic-write geometry. Product **0.0.004**. `earned_next=false`.
