# Changelog

## 2026-08-17

- **TM.0.23.CORTEX.V9 apparatus:** Scorer-only D1 press/harm bind matching C5. Frozen commitment, no neural edit, no candidate SHA. [`cortex_v9.prereg.lock`](cortex_v9.prereg.lock). `earned_next=false`; `ex0s=null`; product **0.0.004**.
- **TM.0.23.CORTEX.V8 diagnosis:** D1 press=0 lives still ACT; they lock onto get/drop which also improves body_adv. C5 already learns press vs harm on two handles. [`cortex_diagnosis.v8.lock`](cortex_diagnosis.v8.lock) authorizes a scorer-only v9 D1 bind of press+harm. No neural change. Do not rescore v8. `earned_next=false`; `ex0s=null`; product **0.0.004**.
- **TM.0.23.CORTEX.V8 gate fail:** Candidate `40680fb` revealed after a clean push (git SHA pinned). Seed-derived D1–D2 **1/16** — **not clear**. D1 extras now match floors (21/32); remaining reds are always-HOLD. D2 14/32. [`cortex_v8_gate.failure.lock`](cortex_v8_gate.failure.lock) + [`cortex_v9.isolation.lock`](cortex_v9.isolation.lock). **DEVELOP.v8 refused.** Do not edit-rescore v8. `earned_next=false`; `ex0s=null`; product **0.0.004**.
- **TM.0.23.CORTEX.V8 candidate:** Reuses frozen v7 neural under the v8 scorer contract. Nine sanity + math audit pass. Population C4/C5/C6 green. [`cortex.candidate.v8.lock`](cortex.candidate.v8.lock). No reveal in this commit. `earned_next=false`; `ex0s=null`; product **0.0.004**.
- **TM.0.23.CORTEX.V8 apparatus:** Scorer-only cycle authorized by v7 diagnosis. Frozen population extras from birth weights, `life_delta_min=0.10`, sealed-seed worlds, reveal must pin git commit. No neural edit. No candidate SHA. [`cortex_v8.prereg.lock`](cortex_v8.prereg.lock). `earned_next=false`; `ex0s=null`; product **0.0.004**.
- **TM.0.23.CORTEX.V7 audit / diagnosis:** Historical 0/16 stands. Append-only [`cortex_v7_gate.audit.lock`](cortex_v7_gate.audit.lock): extras were single-life strict `>` after partial teach; birth vs trained probe types differ; sealed seed did not generate worlds; reveal missed git commit SHA. [`cortex_diagnosis.v7.lock`](cortex_diagnosis.v7.lock): C4 retained; population C5/C6 already green; sampled D1 press=0 lives still ACT on frozen probes. No neural v8 change authorized. Do not rescore v7. `earned_next=false`; `ex0s=null`; product **0.0.004**.
- **TM.0.23.CORTEX.V7 statistical motor contract → gate fail:** Canonical baseline `97691cd`. V6 remains frozen (D1–D2 7/16; boundary contract 6/8; C4 green; no DEVELOP.v6). Frozen [`cortex_v7_stat_contract.lock`](cortex_v7_stat_contract.lock): paired C5/C6 population tests; D1/D2 floors unchanged plus trained > birth and trained > plasticity-off plus D2 association. Observational v6 diagnosis: C4 revision real; contract-honest population C5/C6 already pass. Authorized v7: retain v6 geometry and C4; skip `W_op` ACT credit when `body_adv≈0`. Population boundary: C4 green; C5 30/32 mean Δ≈0.24; C6 inside band; historical lock **7/8** (single-life C7 red, not rewritten). Fresh sealed D1–D2 **0/16** — **not clear**. [`cortex_v7_gate.failure.lock`](cortex_v7_gate.failure.lock) + [`cortex_v8.isolation.lock`](cortex_v8.isolation.lock). **DEVELOP.v7 refused.** Do not edit-rescore v7 on revealed worlds. Full D0–D12 not opened. `earned_next=false`; `ex0s=null`; product **0.0.004**.
- **TM.0.23.CORTEX.V6 audit:** Historical [`cortex_mact_boundary.v6.lock`](cortex_mact_boundary.v6.lock) claimed 8/8 green. Contract-honest reread: **C4** frozen swap timing holds (stale counts == before; then B). **C5** press=10>harm=9 under plasticity-off (pass used a substituted `W_act_query` rule after `score_d1` was green). **C6** press=6>harm=4 in both bind orders under the frozen D1-shaped bar (pass used an 8–0 bar after a prior red). Append-only [`cortex_mact_boundary.v6.audit.lock`](cortex_mact_boundary.v6.audit.lock). Runner C5/C6/C8 restored to contract for later cycles. `--verify-v6-gate` pins failure + honest-red audit + no DEVELOP.v6. Historical locks not rewritten. `earned_next=false`; `ex0s=null`; product **0.0.004**.
- **TM.0.23.CORTEX.V5 diagnosis → V6 → gate fail:** Frozen [`cortex_diagnosis.v5.lock`](cortex_diagnosis.v5.lock): no-consequence 8–0 was first-bound / first-sampled-vector bias plus uniform ACT-cost credit; swap “stale” probes leaked physics; 40 post-swap episodes did not revise. V6 (authorized only by that lock): handle-keyed unit motor vectors, zero `W_act_query` until body evidence, snapshot credit, skip motor-query update when `body_adv≈0`, bind-order-independent tie-break. Boundary **8/8** ([`cortex_mact_boundary.v6.lock`](cortex_mact_boundary.v6.lock): frozen immediate probe stayed A, then revised to B). Fresh sealed D1–D2 **7/16** — **not clear**. [`cortex_v6_gate.failure.lock`](cortex_v6_gate.failure.lock) + [`cortex_v7.isolation.lock`](cortex_v7.isolation.lock). **DEVELOP.v6 refused.** Do not edit-rescore v6 on revealed worlds. Full D0–D12 not opened. `earned_next=false`; `ex0s=null`; product **0.0.004**.
- **TM.0.23.CORTEX.M_act → V5 audit:** Historical [`cortex_mact_boundary.v5.lock`](cortex_mact_boundary.v5.lock) claimed 8/8 green. Contract-honest reread: **C4** flipped immediately after swap (stale_ok required); **C6** press=8>harm=0 under the frozen D1-shaped bar (pass used a raised bar). Append-only [`cortex_mact_boundary.v5.audit.lock`](cortex_mact_boundary.v5.audit.lock). Runner C4/C6 restored to contract for later cycles. `--verify-v5-gate` pins failure + no DEVELOP.v5. Historical locks not rewritten. `earned_next=false`; `ex0s=null`; product **0.0.004**.
- **TM.0.23.CORTEX.M_act → V5 → gate fail:** Frozen M_act boundary contract + v1 runner; scored planted v4 with expected reds ([`cortex_mact_boundary.lock`](cortex_mact_boundary.lock)). V5 apparatus (amendment, boundary runner.v2, gate runner, sealed narrow commitment) then `bind_actuators([opaque_handle_id,…])` + internal motor-registry RNG; ACT credit uses motor vectors (no sensory leak). Nine sanity → birth → audit → [`cortex.candidate.v5.lock`](cortex.candidate.v5.lock). Boundary lock claimed 8/8 (see audit). Narrow D1–D2 re-earn on fresh worlds **4/16** — **not clear**. Frozen [`cortex_v5_gate.failure.lock`](cortex_v5_gate.failure.lock) + [`cortex_v6.isolation.lock`](cortex_v6.isolation.lock). **DEVELOP.v5 refused.** Do not edit-rescore v5 on revealed worlds. `earned_next=false`; `ex0s=null`; product **0.0.004**.

## 2026-08-16

- **TM.0.23.CORTEX.DIAG→V4 audit:** fixed diag `b_op`-aware softmax; refuse clobber of v1 `cortex_diag.lock` when diagnosing later candidates; machine math audit + `--verify-v4-gate` (pair-clear consistency, no soft D1/D2, refuse gate rewrite); CI verifies v4 gate. Retest **13/16** clear matches frozen lock. Product **0.0.004**.
- **TM.0.23.CORTEX.DIAG → V4 D1–D2 gate:** Trace-pure DIAG on frozen v1 (motor lexicon absent; ACT cost dominated advantage; forced-HOLD). Apparatus frozen before edits. Isolated cycles: v2 gate **2/16** fail → v3 **11/16** fail (plastic `b_op` killed HOLD) → v4 **13/16** **clear** with frozen `b_op[ACT]=0.85`, `M_act={press,harm}`, `OP_COST[ACT]=0.05`. Original architecture contract untouched. Full D0–D12 deferred to a later fresh commitment. `earned_next=false`; `ex0s=null`; product **0.0.004**. [`tm023cortex_v4_gate_results.md`](tm023cortex_v4_gate_results.md).
- **TM.0.23.CORTEX.DEVELOP scorer audits (R1+R2):** closed false-positive gates (D3–D7/D9 noise; D4 strip-without-recall; D11 rename/`both_fail_d8`; D5 margin; D12 weight-hash / S-pollution / vacuous 0==0 skill). v1–v3 locks preserved; final rescore **0/16** pair-clear (D12 0; D11 4; D9 12). Neural unchanged. [`cortex_scorer_audit.amendment.lock`](cortex_scorer_audit.amendment.lock) · [`cortex_scorer_audit.r2.amendment.lock`](cortex_scorer_audit.r2.amendment.lock).
- **TM.0.23.CORTEX.DEVELOP** scores D0–D12 on the frozen `915049c` candidate with no neural edits. Append-only [`cortex_sanity_spec.amendment.lock`](cortex_sanity_spec.amendment.lock) (eight learning-law tests + `cpu_gpu` accelerator; dual `advantage_path` assertions). Runner frozen before reveal; eval seed published in [`cortex_eval_reveal.lock`](cortex_eval_reveal.lock); composition [`cortex_development.prereg.lock`](cortex_development.prereg.lock). Battery **0/16** pair-clear; `development_gate_clear=false`; `eligible_for_000005=false`; `earned_next=false`; `ex0s=null`; **product 0.0.004**. Capacity + neural parity wall diagnostic only. [`docs/tm023cortex_results.md`](tm023cortex_results.md).
- **TM.0.23.CORTEX** developmental artificial cortex (birth pass): new organism (`make_cortex` / `NeuralCortex`) — sequential sensory microticks, directed prediction credit, body_state from world physics, internal motor loop (RETRIEVE/WRITE/EMIT/ACT/STOP/HOLD), retrieval buffer, separate source vectors. Worlds + 256-bit eval commitment + prereg/wall before impl. Unscored learning-law sanity **pass** on CPU + Spark GPU (torch 2.13.0+cu130 / NVIDIA GB10). Birth + candidate locks; **no D0–D12 scoring**. Does not wrap `make_interpret` / `ThreeMemoryAgent`. Product remains **0.0.004**; `earned_next=false`, `ex0s=null`. [`docs/tm023cortex_results.md`](tm023cortex_results.md).
- **TM.0.22.INTERPRET** behaviorally evidenced interpretation (prereg-repaired): opaque `observe_source_consequence` with pre-outcome `interaction_token`; independently grounded behavioral anchors; dual use-time outputs UNIQUE/AMBIGUOUS/INSUFFICIENT + SUPPORTED/CONFLICT/UNKNOWN; factorized reconstruct (no Jaccard); never promotes interpretation to world ANSWER; recipient repair via `goal_cue_symbols`; anti-memorization composition; strip/donor/causality cells. Phase A perspective-on / interpretation-off baseline; Phase B unit cells **16/16**; Phase C J0–J16 + twin + independent capacity forks (`life_last_stage_clear=J15`). Wall prereg before candidate; scored scripts pass; executed `W_claim_understand` → `diagnostic_fail` / **honesty** (cause UNKNOWN scorer/narrative only). Audit: exact banned-token match (no substring false positives); refuse unequal message/action lengths (no truncated zip); fit requires equal lengths; capacity never forces `ok=True` on metric-only rungs; v1 candidate preserved. Product remains **0.0.004**; `earned_next=false`, `ex0s=null`. Not subjective comprehension / Ex0S 1.0. [`docs/tm022interpret_results.md`](tm022interpret_results.md).
- **TM.0.21.PERSPECTIVE** source exposure, evidenced perspective, report alignment (prereg-repaired): closed-world exposure scaffold; `observe_exposure` / `experience_perspective`; use-time ALIGNED|MISALIGNED|UNKNOWN; exact event linkage (not Jaccard attach); frozen influence (world-unique first; MISALIGNED withhold, never invert); stacks on RELIABILITY via `make_perspective`. Phase A reliability-on / perspective-off baseline; Phase B unit cells **9/9**; Phase C P0–P12 + twin + unconfounded capacity. Wall prereg before candidate; scored scripts pass; executed `W_misunderstood` → `diagnostic_fail` / **comprehension**. Audit: world-first before predictive margin; donor-exposure causality forks; repetition margin dedup by evidenced perspective. Product remains **0.0.004**; `earned_next=false`, `ex0s=null`. Not honesty_score / belief / Ex0S 1.0. [`docs/tm021perspective_results.md`](tm021perspective_results.md).
- **TM.0.20.RELIABILITY** evidence-source calibration (prereg-repaired): context-conditioned **source_evidence_margin** (predictive accuracy) from organism-derived claim↔outcome compares — not a `trust_score`. Phase A INQUIRE-on / reliability-off baseline HOLD on conflict; Phase B `use_source_reliability` / `observe_testimony` / append-only `experience_reliability` (λ=4, n_min=2, Jaccard 0.5); opt-in R10 liveness; unit cells **6/6**; Phase C R0–R12 + twin + unconfounded capacity. Wall prereg before candidate; scored wall scripts pass; executed `W_competent_liar` → `diagnostic_fail` / **honesty**. Audit: live-supersede persist; `testimony_derived` excluded from grounding support; donor/isolation forks real; capacity N-way honest. Product remains **0.0.004**; `earned_next=false`, `ex0s=null`. Not Ex0S 1.0. [`docs/tm020reliability_results.md`](tm020reliability_results.md).
- **TM.0.19.INQUIRE** active evidence acquisition: Phase A SEQUENCE-on baseline (confident ANSWER / ambiguous HOLD, inquire off); Phase B default-off `use_inquire` / `plan_inquiry` / `experience_inquire` (one-step minimax partition value + locked cost; host executes probes; dual memory); unit cells **6/6**; Phase C I0–I12 + twin + unconfounded capacity (hyps 2→16, depth 1→4, age 100→10k, sources 1→4). Wall prereg before candidate; scored wall probes pass; `first_fail_wall=W_conflict_teachers` (source reliability). Product remains **0.0.004**; `earned_next=false`, `ex0s=null`. Not Ex0S 1.0 / route LOOKAHEAD. [`docs/tm019inquire_results.md`](tm019inquire_results.md).
- **TM.0.18.SEQUENCE** grounded symbolic expression package: Phase A baseline select-OK / construct-HOLD on `make_symbol_ground`; Phase B default-off `use_symbol_sequence` / `experience_sequence` with evidenced STOP, atomic HOLD, factorized context (no scene IDs); unit cells **6/6**; Phase C expressive life E0–E12 + reverse-order twin + unconfounded capacity lanes (vocab **8→32→128→512**); dialogue wall preregistered before candidate score. Cleared through **E12** (life probes through **E11**; E12 = capacity launch). Audit: single-atom compose only; honest `life_last_stage_clear`; no partial-credit leak. Product remains **0.0.004**; `earned_next=false`, `ex0s=null`. Not Ex0S 1.0. [`docs/tm018sequence_results.md`](tm018sequence_results.md).
- **TM.0.17.SYMBOLWORLD** grounded symbolic learning package: Phase A freezes an unfamiliar symbolic world and baseline HOLD on PERSIST-on; Phase B adds one default-off evidence-weighted `use_symbol_ground` / `experience_grounding` candidate (no POS-specific learners; unit cells **6/6**); Phase C one continuous developmental life S0–S10 + renamed-language twin. Cleared through **S10**. Product remains **0.0.004**; `earned_next=false`, `ex0s=null`. Not Ex0S 1.0. [`docs/tm017symbolworld_results.md`](tm017symbolworld_results.md).
- **TM.0.16.LIFEWALL** continuous-lifetime integration wall on frozen PERSIST-on: one accumulating life + causally remapped twin; RELATE, ALIASFINGER, and mark-continuity interleaved; trajectory checkpoints and forks (ρ / strip-C / strip-FP); capacity rungs 4→8→16→32. Cleared through **32**. No `agent.py` change. `earned_next=false`, `ex0s=null`. [`docs/tm016lifewall_results.md`](tm016lifewall_results.md).
- **TM.0.16.PERSIST** opt-in mark-continuity persistence under the frozen continuity-evidence contract: exact-key `observe_continuity_mark`; bilateral uniqueness (exactly one apply P and one matching read Q); use-time one-hop projection of an existing skel edge; no stored token map. C0 reuses GAPWALL lessons; C1 weak HOLD; C2 identity-dependent earn; C3 triad refuses unique; C4 follows swapped marks; C5 withdraws on later state / second read / second apply and reprobes; C6 evidence lives in S only. Battery **9/9**. Product remains **0.0.004**; `earned_next=false`, `ex0s=null`. Candidate lock before score. Audit repair: phase–op enforced on recompute; multi-token cue fail-closed; strict HOLD (wrong dest is not HOLD); cells emit frozen prereg fixtures; fingerprint rows inaccessible; v1 candidate superseded immutably. [`docs/tm016persist_results.md`](tm016persist_results.md).
- **Continuity-evidence contract preregistered** (no numeric lab ID): provisional post-gap identity requires a causal pre-gap apply and exactly one matching post-gap readout; empty-skip, sole candidacy, spelling, and bare `mark_id` are insufficient; C3 triad refuses unique choice; C5 mandates withdrawal; C2 splits admissibility from future identity-dependent earn. No persistence mechanism, runner, or earn this pass. Product remains **0.0.004**; `earned_next=false`, `ex0s=null`. [`docs/continuity_evidence_contract.md`](continuity_evidence_contract.md).
- **TM.0.16.GAPWALL** continuity capacity wall on frozen ALIASFINGER-on: adjacent route baseline; empty-event skip bridge (explicitly not object continuity); episode boundary removes the frontier; distractor enters the route; one-candidate reappearance measured unique by skip semantics; two equal candidates tie and HOLD. Battery **6/6**. No `agent.py` change. `earned_next=false`, `ex0s=null`; not TM.0.17 / 0.0.005. [`docs/tm016gapwall_results.md`](tm016gapwall_results.md).
- **TM.0.16.ALIASFINGER** opt-in behavioral fingerprints under frozen alias-evidence contract: exact-key `observe_alias_probe`; pairwise-qualified cliques; derived compose projection (raw `experience_skel` untouched). Battery **7/7**. `earned_next=false`, `ex0s=null`. Candidate lock before score. Audit repair: ambiguous clique → HOLD (no raw fallback); v1 candidate superseded immutably. Not TM.0.17 / 0.0.005. [`docs/tm016aliasfinger_results.md`](tm016aliasfinger_results.md).
- **Alias-evidence contract preregistered** (no numeric lab ID): behavioral fingerprints require matching observable consequences under at least two independent probe contexts; one witness and route position are insufficient. A0–A6 outcomes frozen only; no candidate mechanism, runner, earn, or freeze lock. Product remains **0.0.004**; `earned_next=false`, `ex0s=null`. [`docs/alias_evidence_contract.md`](alias_evidence_contract.md).
- **TM.0.16.ALIASWALL** wall probe on frozen RELATE: Control (stable names) selects route; Kill (preregistered opaque aliases) fragments support → HOLD. Isolation per cell; no alias machinery; not object continuity. Battery **6/6**. `earned_next=false`, `ex0s=null`. [`docs/tm016aliaswall_results.md`](tm016aliaswall_results.md).
- **TM.0.16.RELATE** candidate relations under ambiguity: `observe_event` reads `visible` only (never `focus`); all-pairs candidate `experience_skel` cloud; repeated lives raise support; compose picks unique evidence winner and HOLDs on ties; losers stay in S; `end_event_episode` clears RELATE transient only. Battery **16/16**. `earned_next=false`, `ex0s=null`. D9 focus-not-oracle; D10 irreducible tie; D11 episode boundary. Prereg [`relate_016.prereg.lock`](relate_016.prereg.lock) before freeze. Not vision; not Platonic map discovery. [`docs/tm016relate_results.md`](tm016relate_results.md).
- **TM.0.15.SKELETON** observed-transition acquisition: apparatus emits `[X,A,Y]` via `observe_symbol`; organism authors `experience_skel` adjacency, then compose+ACQUIRE contextual continuation over that path. Dual-strip causality; reachability invariants; competing support (no invented contradiction). Battery **16/16**. `earned_next=false`, `ex0s=null`. Audit: D7 evidence_tie; D11 real order permute; D12 teacher keys; D1/D6 causality; ledger across cells. Prereg [`skeleton_015.prereg.lock`](skeleton_015.prereg.lock) before freeze. Not latent map discovery. [`docs/tm015skeleton_results.md`](tm015skeleton_results.md).
- **TM.0.14.FAMILY** kill-or-earn on frozen ACQUIRE: **288/288** generated developmental histories (A–D 144/144; E–H 144/144 sealed). Provenance ledger; full World manifests. `earned_next=true`, **`ex0s=null`** (no product stamp named). Audit: no invented lives; behavioral CF; oracle press↔κa/tune↔κb; hop depth; earn pins lock row SHAs + seed. [`docs/tm014family_results.md`](tm014family_results.md).
- **TM.0.14.ACQUIRE** (candidate): experience authors contextual continuations (`source=experience_ctx`) over a planted untagged skeleton. Apparatus never writes `ctx=`. One-shot lived compose κ. Brutal battery **16/16**. [`docs/tm014acquire_results.md`](tm014acquire_results.md).

## 2026-08-15

- **Ex0S 0.0.004 — Contextual Composition** (TM.0.13.FAMILY): frozen CONTEXT-on recipe vs **288 planted contextual worlds**. **288/288** (A–D 144/144; hold-out E–H 144/144). E–H sealed via world manifests until canonical run. Genome delta 0. One holdout-manifest regeneration after F/H evidence-ladder generator fix — earn run used **fresh sealed hold-out worlds** (not an organism change). [`docs/tm013family_results.md`](tm013family_results.md).
- TM.0.13.FAMILY apparatus: A–D develop; E–H cryptographically committed; CI A–D smoke + E–H sealed checks only.
- TM.0.13.CONTEXT: first cognitive function in M. Freeze κ (`kappa_013.lock`, `ctx_encoding=ksem-sha256-v1`); carry `(Y,κ)` after non-motor hops; plant ctx via independent `reference_route_kappa`; evidence-causal family **14/14** (post-audit). `genome_011.lock` immutable. [`docs/tm013context_results.md`](tm013context_results.md).
- Renamed public repo to [mpiekarski78/Ex0S](https://github.com/mpiekarski78/Ex0S) (was `three-memory`). Lab IDs stay `TM.*`.
- TM.0.11.FAMILY (**Ex0S 0.0.003 — Frozen Composition**): frozen compose genome vs **252 generated external-state worlds**. **252/252** (A–D 144/144; hold-out E–G 108/108; hold-out depth 4). First-hop D/F only (no lookahead). `no_transitive_shortcuts`. G upstream body hash stable. Genome changes 0; apparatus interventions 0. Hold-out generators preregistered in [`genome_011.lock`](genome_011.lock). [`docs/tm011family_results.md`](tm011family_results.md).
- License: Apache-2.0 for code ([`LICENSE`](../LICENSE)); [`NOTICE`](../NOTICE) names Marcin Piekarski and Ex0S; [`CITATION.cff`](../CITATION.cff) for credit. Ex0S name/logo stay project identity (not licensed marks).
- TM.0.12.ROUTESIG.IDENTITY: C12A opaque fid rename vs C12B same-fid last-hop rewrite on frozen C10. Kfid differs/same; Ksem same/differs. Same rolling F; payload is the question. Ksem = `canonical(bind, did)` only. Not: Kfid fails cognition; alpha-rename; 0.0.004. Next: freeze κ contract, then CONTEXT in M. [`docs/tm012routesig_identity_results.md`](tm012routesig_identity_results.md).
- ROUTESIG.IDENTITY audit: both traces; C12B exactly-one inherited rewrite; ORDER `kappa_*` SHA cross-pin; unique fids; shuffle invariance. Scientific table unchanged.
- TM.0.12.ROUTESIG.DEPTH: one shared S; C11[k] frontiers at `T_k` (k=1..8). Geometric R2/R2x assert; R1/R2 **collision** ×8; R2x/R3/R4 distinguish ×8. Claim: each tested fixed window of size k fails when the distinction is k+1 back; rolling κ retains it (constant 256 bits). C11[2]~C10 suffix-2 pattern only. No rewrite of ORDER lock; no 0.0.004. Next: identity/rename robustness of κ. [`docs/tm012routesig_depth_results.md`](tm012routesig_depth_results.md).
- ROUTESIG.DEPTH audit: pin `TraceSpec`; refuse both motors; fail-closed C11[2] pattern; enforce distinct frontiers + path⊆S; live extract/`path_and_frontier` + κ source guards. Scientific table unchanged.
- TM.0.12.ROUTESIG phase 1 (order): C10 commutable loops — same unordered path-edge fid set, different order. R0/R1/R2 **collision**; R3/R4 distinguish. Claim: traversed-relation **set** insufficient; **order** necessary; output-blind incremental κ can carry the distinction without the raw path — not SHA-as-genome / full-path / 0.0.004. Audit: global unique edges; path-fid multiset; `TraceSpec` pin. Next: ROUTESIG.DEPTH. [`docs/tm012routesig_results.md`](tm012routesig_results.md).
- TM.0.12.MIDPATH: same-S C9 (length-4; one interior token). H3c **collision**; H3b distinguishes; H0/H1/H2/H3a/H4 collide. Claim: endpoint provenance insufficient; interior route info necessary — do **not** conclude store-the-full-path. H3c MIDPATH-local; raw-trace refuse; helper SHA pins (+ `TraceSpec`); unique shared `P→Y`. No 0.0.004. [`docs/tm012midpath_results.md`](tm012midpath_results.md).
- TM.0.12.PATHDISC: same-S origin vs path (C8 diamond; two apparatus traces). H3a **collision**; H3b/H2/H4 distinguish. Claim: origin alone insufficient; some route info beyond origin necessary — do **not** conclude store-the-full-path. Audit: Y→motor validation; same-S edge subset; `scorer_sha` pin; **runtime seed fail-close** (lock validation). [`docs/tm012pathdisc_results.md`](tm012pathdisc_results.md).
- TM.0.12.MINIMAP: representation distinguishability on locked CONTEXT contrasts (no organism change). Least-structured sufficient **candidate** on C2–C6: `(token, origin)`; C7 `UNOBSERVABLE_FROM_PROVENANCE`; H4 incoming-fid diagnostic (collides on C4). Audit: C7 vacuous short-circuit fixed; motor fail-closed; answer-fid refuse. [`docs/tm012minimap_results.md`](tm012minimap_results.md).
- Freeze audit: `verify_freeze` now **fail-closes on `agent_sha`** (compose lives in `agent.py`); tests no longer rewrite locks; lock pins `baseline_commit` `c392aa515b7a3445bb15bc55ad969d971632ea3f`. Rewrite only via `--write-lock`. [`docs/CURRENT_ORGANISM.md`](CURRENT_ORGANISM.md).
- TM.0.11.BOUND: capacity envelope of frozen 0.0.003 — 1-D sweeps, four-class outcomes, preregistered expected boundaries (local-opt HOLD; reuse-A bind-only). Within-model **42/42**; expected boundaries **2/2**; unexpected **0**; no Ex0S 0.0.004. Audit: fixed non-discriminating reuse-A; honest file_order annotation; per-probe timeout. [`docs/tm011bound_results.md`](tm011bound_results.md).
- TM.0.12.CONTEXT: representation audit of provenance lost at bare-token compose frontiers (H0–H4; C0–C7 incl. indistinguishability witness). Within-model **2/2**; expected boundaries **7/7**; C7 witness confirmed; unexpected **0**; no Ex0S 0.0.004; no genome patch. LOOKAHEAD and \|S\| profile stay separate ([`tm011bound_perf.md`](tm011bound_perf.md)). [`docs/tm012context_results.md`](tm012context_results.md).

## 2026-08-14

- Created public repo [mpiekarski78/Ex0S](https://github.com/mpiekarski78/Ex0S) (originally published as `three-memory`; not a BDH fork).
- Scaffold: frozen cortex, session ρ, inspectable store S, innate novelty/integrity drives.
- v0 key/door world + CLI `experiments.run_v0`.
- Reported classification: **Store-works**. disable-S recovers Trace-only (BDH Category B analogue).
- Audit: removed env-injected lesson strings; writes are event-driven. Documented tag→action retrieve limit. Metrics include `n_forced_steps`.
- v1 tiny byte LM + NOTE retrieve; same `my lo` probes as BDH. Classification **Store-works**. Published BDH numbers are the trace-only column ([`docs/v1_results.md`](v1_results.md)).
- v2: plain prior (no NOTE-copy, follow-acc 0.025) + raw snippet retrieve. Classification **Trace-only**. S is inspectable and prepended as `my love\nmy lo`; after ρ reset P(v) stays at prior. v1 Store-works needed the taught copy protocol.
- v3: S is a folder of `.md` files (no embeddings). New agent + empty ρ + reload from disk. note arm **Store-works** (P(v)=0.988, JS vs in-process = 0). raw arm **Trace-only** (P(v)=0.093). Files persist; the LM still only *uses* them with the taught NOTE protocol.
- v4: 13 notes in S, select longest heading vs dump-all. note+select **Store-works** (P(v)=0.988, 12 rejected). Dump-all **collapses** P(v) to 0.007. raw+select **Fail** (file unused). [`docs/v4_results.md`](v4_results.md).
- v5: unread library W. commit copies one file into S then unmounts W: note **Store-works** (P(v)=0.988, S=`my-lo.md` only). Peek then unmount → prior. Collect off ignores W. raw commit **Fail**. [`docs/v5_results.md`](v5_results.md).
- v6: plain prior only. **tool** grammar (heading→byte, bias +3.0) **Store-works** P(v)=0.649; LM context is `my lo` (no `love`). fewshot and untaught NOTE **Fail** (~0.053). Use-skill belongs in machinery, not in cortex. [`docs/v6_results.md`](v6_results.md).
- v7: native integer tags, no English prior. Genome = cortex seed 1337 (not ACGT). Experience + reload `.tag` files **Store-works** (`use_key`). Collect commit unmount works; peek unmount and dump-all return `open`. [`docs/v7_results.md`](v7_results.md).
- v8: boxed use-policy may learn; cortex frozen. Features `{s_hit, w_hit}` only. Two-step train (commit then apply after unmount). Red unmount `use_key`; held-out green `wait`; disable-S / empty S `open`. **Store-works**. [`docs/v8_results.md`](v8_results.md).
- v9: write from a life, not from W. Clutter library has no `d0.tag`/`d2.tag`. Policy learns when to author `{door, action}` from a door-opening. Red → `d0.tag` `use_key`; held-out green → `d2.tag` `wait`. **Store-works**. [`docs/v9_results.md`](v9_results.md).
- v10: free life, no forced curriculum. ε-greedy over percept affordances; probe greedy. `n_forced=0`. Red sequence was not OPEN→PICK→USE; green found WAIT. **Store-works**. [`docs/v10_results.md`](v10_results.md).
- v11: two free lives, one S. Authored `d0.tag`+`d2.tag`. Select red `use_key` / green `wait`. Dump-all red **`wait`**. **Store-works**. [`docs/v11_results.md`](v11_results.md).
- v12: retrieve head learns select vs dump (`{n_store, n_hits}`). Untrained dumps red `wait`; trained selects. Held-out blue `open`. Dump-all still fails. **Store-works**. [`docs/v12_results.md`](v12_results.md).
- v13: use-gate + generic `logits[int(action)] += 3.0`. Untrained ignores a planted tag (`open`). Trained red `use_key`; held-out green `wait`. **Store-works**. [`docs/v13_results.md`](v13_results.md).
- v14: A pick-one among same-door matches vs B write `{door}` vs `{door, action}`. Both **Store-works**. [`docs/v14_results.md`](v14_results.md).
- v15: joint write/schema/use/pick with no clamps. **Store-works**. [`docs/v15_results.md`](v15_results.md).
- v16: A prefer `ok=1` over newest junk (**Store-works**); B shared return on v15 joint (**Fail**). [`docs/v16_results.md`](v16_results.md).
- v17: A read `do=` vs `action=`; B match `here=` vs `door=`. Both **Store-works**. [`docs/v17_results.md`](v17_results.md).
- v18: A write `do=` vs `action=`; B write `here=` vs `door=`. Read/match frozen. Both **Store-works**. [`docs/v18_results.md`](v18_results.md).
- Audit: unknown policy traces no longer train the collect head; `clone_empty` keeps boxed-head flags; v18 now probes empty-S green. Retest still **Store-works** / **Store-works**.
- v19: A shared value-name (`do=` vs `action=`); B shared place-name (`door=` vs `here=`). Neither side frozen. Both **Store-works**. [`docs/v19_results.md`](v19_results.md).
- v20: A find unread W (`p99.tag` `{here, action}`); B find vs `door=` junk. Frozen v5 commit; use-gate + match learn. Both **Store-works**. Audit: match now runs before collect; collect no longer wipes `match_alt`. [`docs/v20_results.md`](v20_results.md).
- v21: A filename-first vs newest among unread `here=` hits; B dump-all vs newest. Query and copy frozen. Recency-swap stays `wait`. Both **Store-works**. [`docs/v21_results.md`](v21_results.md).
- v22: A complete vs stub (no planted `when=`); B joint match+wsel+use with no clamps. Both **Store-works**. [`docs/v22_results.md`](v22_results.md).
- v23: A joint match+complete+use, no `when=` (**Store-works**); B shared return on the same stack (**Fail**, last-50 0, no policy updates). [`docs/v23_results.md`](v23_results.md).
- TM.0.1.0: open query names from files, not `{door, here}`. A split and B shared return both **Store-works**. [`docs/tm010_results.md`](tm010_results.md).
- TM.0.1.1: open copy names from the hit, not `{action, do}`. Query frozen to the files’ place key. A split and B shared return both **Store-works**. [`docs/tm011_results.md`](tm011_results.md).
- TM.0.1.2: messy retrieve — rank unread files; exact `loc=`/`door=` misses. A split and B shared return both **Store-works**. Audit: `where=` is not `here=`. [`docs/tm012_results.md`](tm012_results.md).
- TM.0.2.0: scale of W — 256 messy unread files; same `{has_code, has_rare}`. A split and B shared return both **Store-works**. [`docs/tm020_results.md`](tm020_results.md).
- TM.0.3.0: a life — free life find/commit; probe after ρ reset with W gone. A **Store-works**; B shared return **Fail** (last-50 0). [`docs/tm030_results.md`](tm030_results.md).
- TM.0.3.1: documents — free life over unread `.md` W (prose + embedded `k=v`). A **Store-works**; B shared return **Fail**. [`docs/tm031_results.md`](tm031_results.md).
- TM.0.3.2: prose retrieve — no filed `action=`/`where=`; digit scan → anonymous `n*`; vname picks motor int. A **Store-works**; B shared return **Fail**. Audit: heading digits excluded. [`docs/tm032_results.md`](tm032_results.md).
- TM.0.4.0: leave the door world — `ChannelDialWorld` + prose free life. A **Store-works** (PRESS / held-out TUNE); B shared return **Fail** (clutter PRESS, C miss). [`docs/tm040_results.md`](tm040_results.md).
- TM.0.5.0: no answer integers — copy innate motor name token, not digits. A **Store-works**; B shared return **Fail** (last-50 0). Developmental rule locked (how to learn / correct; English then math as lives). [`docs/tm050_results.md`](tm050_results.md).
- TM.0.5.1: correct a wrong commit — drop junk S, retry, keep after ρ reset. Search frozen so correction is load-bearing. A **Store-works**; B shared return **Fail**. [`docs/tm051_results.md`](tm051_results.md).
- TM.0.5.2: unread W does not name the motor — stamp the act from the event onto a rare committed note. A **Store-works**; B shared return **Fail**. [`docs/tm052_results.md`](tm052_results.md).
- TM.0.5.3: use the fact, not pick-a-motor — same S, A PRESS / C HOLD; copy-only still PRESS on C. A **Store-works**; B shared return **Fail**. [`docs/tm053_results.md`](tm053_results.md).
- TM.0.5.4: Open W — distinct multi-paragraph unread pages, not cloned one-liners. Same find/stamp/here-match. A **Store-works**; B shared return **Fail**. [`docs/tm054_results.md`](tm054_results.md).
- TM.0.5.5: accumulate S — two lives, same store; after both, A PRESS / C TUNE. Wipe-between loses A. A **Store-works**; B shared return **Fail**. [`docs/tm055_results.md`](tm055_results.md).
- TM.0.5.6: never-wipe train — dirty S survives 500 episodes; C life on that S adds TUNE without losing A. A **Store-works**; B shared return **Fail**. [`docs/tm056_results.md`](tm056_results.md).
- TM.0.5.7: find without a unique rare token — several hapax clutter pages; same never-wipe store. A **Store-works**; B shared return **Fail** (C stays HOLD). [`docs/tm057_results.md`](tm057_results.md).
- TM.0.5.8: scale of Open W — 64 distinct document-shaped clutter pages; same multi-rare never-wipe recipe. A **Store-works**; B shared return **Store-works** (not retuned). [`docs/tm058_results.md`](tm058_results.md).
- TM.0.5.9: correct the dirty store — stop committing once S names here; drop unstamped pages after a real stamp. A **Store-works** (S n=1); B shared return **Fail**. [`docs/tm059_results.md`](tm059_results.md).
- TM.0.6.0: first English life, tiny corpus — bind a page word from the event (`did=` bookkeeping, copy `push`/`adjust`). A **Store-works**; B shared return **Store-works** on this slice (not retuned). [`docs/tm060_results.md`](tm060_results.md).
- TM.0.6.1: one bind per note — distractor hapax on the same page does not fire. A **Store-works**; B shared return **Store-works** on this slice (not retuned). [`docs/tm061_results.md`](tm061_results.md).
- TM.0.6.2: never-wipe English on the one-bind recipe — dirty train S still PRESS from `push`; C life on that S missed TUNE. A **Fail**; B shared return **Store-works** on this slice (not retuned). [`docs/tm062_results.md`](tm062_results.md).
- TM.0.6.3: new-here stamp — a second station gets an unmarked rare page on the dirty English store. A **Store-works**; B shared return **Store-works** on this slice (not retuned). [`docs/tm063_results.md`](tm063_results.md).
- TM.0.6.4: English find without a unique rare token — hapax clutter so `has_rare` is not a needle at `p99`. A **Fail** (clutter hapax bound as acts); B shared return **Store-works** on the motor bar (not retuned). [`docs/tm064_results.md`](tm064_results.md).
- TM.0.6.5: concurrent bind — stamp the attended page, block extra hapax at this station. A **Store-works** (`bind=push` only); B shared return **Fail** (first CS `neon`; C miss). [`docs/tm065_results.md`](tm065_results.md).
- TM.0.6.6: correct the dirty English store — here-only + sweep. A **Fail** (train S n=1 `bind=push`; C bound `xenon` not `adjust`); B shared return **Store-works** on the motor bar (not retuned). [`docs/tm066_results.md`](tm066_results.md).
- TM.0.6.7: in-hand new-here — a new station stamps the attended rare page, not the first leftover rare in W. A **Fail** (C bound `neon` in-hand, not `adjust`); B shared return **Store-works** on the motor bar (not retuned). [`docs/tm067_results.md`](tm067_results.md).
- TM.0.6.8: find-novel — search keeps unread pages with the most rare tokens S lacks, and attends that page without stamp-collecting leftover hapax. A **Store-works** (`push` then `adjust`); B shared return **Store-works** on this slice (not retuned). [`docs/tm068_results.md`](tm068_results.md).
- TM.0.6.9: find-novel without a unique two-rare pair — several clutter pages match the novel-count of `p99`/`p98`. A **Fail** (train bound `neon`, C bound `xenon`); B shared return **Store-works** on the motor bar (not retuned). [`docs/tm069_results.md`](tm069_results.md).
- TM.0.7.0: retry-novel — do not lock find on the first max-novel page; keep unread pages that still add a rare token vs the whole library. A **Fail** (n=4, `push` plus clutter hapax); B shared return **Store-works** on the motor bar (not retuned). [`docs/tm070_results.md`](tm070_results.md).
- TM.0.7.1: local-alias — bind→did only from the retrieved note. A **Fail** (retrieve used `xenon`); B shared return **Store-works** on the motor bar (not retuned). [`docs/tm071_results.md`](tm071_results.md).
- TM.0.7.2: keep-steerer — after success, drop other same-here notes. A **Fail** (kept `xenon`, dropped `push`; C used `neon`); B shared return **Store-works** on the motor bar (not retuned). [`docs/tm072_results.md`](tm072_results.md).
- TM.0.8.0: scale English Open W — 64 distinct pages, 16 two-rare clutter. A **Fail** (retrieve used `xenon`; C used `krypton`); B shared return **Store-works** on the motor bar (not retuned). [`docs/tm080_results.md`](tm080_results.md).
- TM.0.8.1: one return is the recipe — split find/mark/use is gone. A **Fail** (retrieve used `xenon`; C used `neon`); B motor bar **Store-works** (not retuned). [`docs/tm081_results.md`](tm081_results.md).
- TM.0.8.2: one machine — body from `n_actions` / percepts, not `domain=`. A **Fail** (retrieve used `xenon`; C used `neon`); B motor bar **Store-works** (not retuned). [`docs/tm082_results.md`](tm082_results.md).
- TM.0.9.0: first math life — search may count unread rares, not `+` in cortex. A **Fail** (retrieve used `xenon`; C used `neon`); B motor bar **Store-works** (not retuned). [`docs/tm090_results.md`](tm090_results.md).
- TM.0.9.1: keep untested competing hypotheses — success of one same-here bind does not delete the other. A **Store-works** (16 supported notes including `push` and `xenon`; retrieve used `thallium`); B motor bar **Fail** (n=17 stamp-collecting; cap not raised). [`docs/tm091_results.md`](tm091_results.md).
- TM.0.9.BOX: boxed-policy leakage control on frozen 0.9.1 genome (delta 0). World-fact leakage not observed (3/3); counterfactual donor Pass (3/3); neutral relevance Fail (3/3). Compatible label **Control Fail**. Cross-world use-S Pass on evaluable stores (2/2); W3 acquisition 2/3 — missing `blen` is not a transfer miss. Historical BOX stays the no-cue leakage control. Next is MATCH, not independent support. [`docs/tm091box_results.md`](tm091box_results.md).
- TM.0.9.2: antecedent MATCH — a stored `X→action` steers only when `X` is in the current stream. A **Store-works** (3/3 permuted nonces; crossed HOLDs). B motor bar **Fail** (no-cue English probe HOLD; n=0; cap not raised). `bind_present_in_current_stream` only; `policy.py` unchanged. [`docs/tm092_results.md`](tm092_results.md).
- TM.0.9.3: EVIDENCE — among MATCH-eligible rivals, prefer earned support. A **Store-works** (unequal follows S; equal HOLD; counterfactual swap). B motor bar **Fail** (no-cue English bar; not retuned). [`docs/tm093_results.md`](tm093_results.md).
- TM.0.9.4: REVISION — same 0.9.3 genome; later outcomes withdraw and reverse preference. A **Store-works** (HOLD→M1→HOLD→M2; mid-reset reverse; order-invariant). B motor bar **Fail** (no-cue English bar; not retuned). [`docs/tm094_results.md`](tm094_results.md).
- TM.0.10.FAMILY (**Ex0S 0.0.001**): frozen 0.9.4 genome vs 252 procedural relation worlds. **252/252** solved (developed A–D 144/144; hold-out E–G 108/108). Genome changes 0. Independent measures all 1.000. Not English, not composition. [`docs/tm010family_results.md`](tm010family_results.md).
- TM.0.11 (**Ex0S 0.0.002**): COMPOSE — chosen non-motor consequent becomes next MATCH frontier. A **Store-works** (2-hop; donors; no shortcut; no residue; S stable). B motor bar **Fail** (no-cue English bar; not retuned). [`docs/tm011compose_results.md`](tm011compose_results.md).
- Ex0S lineage table: [`docs/ex0s.md`](ex0s.md).
- TM.0.11.FAMILY (**Ex0S 0.0.003**): see 2026-08-15 entry above.
