# Competitive plasticity at P1 — TM.0.26.COMPETITIVE

**Lab:** TM.0.26.COMPETITIVE

Not a lineage rescore. Not a capability earn. Not another solver MAP. Product **0.0.004**. `earned_next=false`. `ex0s=null`.

Authorized by [`cortex_v33.prereg.lock`](cortex_v33.prereg.lock). v32 TWOSCALE evidence (`architectural_wall_acquire`, 7/8 eight-cue) remains historical. This package scores **geometry-only** rival-mean competitive plasticity on unused worlds.

## Law

See [`cortex_v33_architecture_amendment.md`](cortex_v33_architecture_amendment.md). Preserve entire v32 organism and triggers. Change only the ACT update vector: positive `(m_h − m_R)`, negative `m_h` only. Tie-mean rival set R uses `TIE_EPS`. No low-margin awake learning.

## Battery

Unused `TM026.COMPETITIVE.DEV.` / `TWIN.`. SCORE reserved. **54 cells** with frozen manifest hash. Pass statistic is normalized geometric margin ≥ 0.01 plus unique ranking and perturbation on stable cells. Acquire records `ranking_ok` and `n_probe_correct == n_cues`.

Integrity gates: negative suppression geometry, actuator permutation, checkpoint continuity, no-teaching → no preference, HOLD inert on `W_act_query` (no pending ACT credit). Secondary scaling: eight cues / four actuators.

Decision ladder uses **competitive_** codes, not TWOSCALE `architectural_wall_*`.

## Refuse

Another MAP; v31 W1; A3; low-margin awake; trigger schedule changes; rewrite TWOSCALE/v32 historical locks; SCORE; `earned_next`; 0.0.005; runner edits after Phase 1 push through DEV.
