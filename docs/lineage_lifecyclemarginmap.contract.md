# Margin-conditioned replay on the frozen L2 lifecycle — TM.0.24.LIFECYCLEMARGINMAP

**Lab:** TM.0.24.LIFECYCLEMARGINMAP  
**Product:** Ex0S **0.0.004**  
**Flags:** `earned_next=false`, `ex0s=null`, `eligible_for_000005=false`  
**Live candidate:** v30, default v29 query scoring.  
**n:** **64**

Not a lineage rescore. Not a capability earn. **Not a neural amendment.** No trace install. No `cortex.candidate.v31.lock`. Do not resurrect W1. Do not open SCORE. Do not test another isolated write formula. Do not rewrite MEMORYLIFECYCLEMAP R2 or V1.

Authorized by [`lineage_memorylifecyclemap.r2.decision.lock`](lineage_memorylifecyclemap.r2.decision.lock) and [`lineage_memorylifecyclemap.r2.decision.addendum.lock`](lineage_memorylifecyclemap.r2.decision.addendum.lock). Historical R2 first-match **episode_reinstatement_match_failure** is preserved and interpreted as **episode_reinstatement_match_failure_under_global_ecological_stored_p1_unique_match_or**. Honest first-match on those cells is **memory_lifecycle_insufficient**.

## How to read the R2 result

`memory_lifecycle_insufficient` means the frozen lifecycle did not pass all four phases. It does **not** mean episodic replay and replacement provided no benefit.

On the published R2 L2 8-cue cells:

- Reversal P1 was found (`reversal_match_recall=1.0`).
- L2 replacement improved reversal relative to L3.
- Eight-cue ranking remained correct.
- Failure was stability strength: live min margin 0.00643 < 0.01, then perturbation failure.

Neither episode matching nor replacement caused that first failure. Error-only replay actually updated **14 / 128** calls. All eight stored signs were first correct at call 20, with **1 / 8** still below 0.01; the remaining **108** calls did nothing. REST **raised** live min margin from 0.00211 to 0.00643. It did not create the weak boundary.

The global σ=0.01 matcher remains a separate limitation: a later candidate cannot claim robust content identity from radius 0.05. It was not the first failure here and must not redirect this diagnosis.

## Question

With the L2 episode store, match radius 0.05, content-addressed replacement, 16-epoch replay schedule, and two live reversal updates held fixed, does changing **only** the replay update trigger — from error-only ranking to the frozen C3 passive-aggressive minimal update toward normalized geometric margin 0.01 — recover the stability gate?

No new η grid, match radius, replay budget, or threshold.

## Arms (frozen)

Runner-only. Exact P1 bridge. Unused `TM024.LIFECYCLEMARGINMAP.DEV.` / `TWIN.`. SCORE reserved.

| Arm | Replay learner | Store |
| --- | --- | --- |
| M0 | Existing error-only L2 control (C1 `η=0.15`, `c_max=1`) | L2 replace |
| M1 | Frozen C3 passive-aggressive (`γ=0.01`, no clipnorm, no η grid) | L2 replace |
| M2 | Same C3 rule | L3 retain-stale (refuse contradictory replacement) |
| M3 | D1 hard-margin oracle on the identical stored rows | L2 replace; ceiling-only; no live reversal training |

M0–M2 receive the same two live reversal updates as R2 L1–L3. M3 is a batch ceiling: one hard-margin fit on valid stored rows, not online replay.

Each acquire/stable cell reports the margin trajectory **acquisition → replay → REST → perturbation**, including first-all-correct call, remaining replay calls after that point, count still below 0.01, and pre/post REST live min margin. Ecological and specificity cells do not run REST; their post-REST fields stay null.

## Phased success contract (frozen)

Same gates as MEMORYLIFECYCLEMAP R2. Acquire is ranking only. Stability requires native ranking margin 0.01 and ranking perturbation σ=0.01 after REST. Bounded match sanity cannot satisfy the stability gate. Ecological σ=0.01 match is reported and is **not** this ladder.

## Decision ladder (disjoint, frozen order)

1. M1 passes all four phases, M0 fails stability, and M2 reversal still passes → `margin_conditioned_replay_supported`
2. M1 passes stability and reversal, and M2 fails reversal → `replacement_and_margin_conditioned_replay_jointly_causal`
3. M1 8-cue live min margin reaches 0.01 before REST and falls below afterward → `consolidation_margin_loss`
4. M1 8-cue never reaches 0.01 and M3 is not four-phase → `compact_margin_replay_insufficient`
5. Only M3 passes all four phases → `max_margin_ceiling_only`
6. Else → `lifecycle_margin_insufficient`

## Refuse

Trace install; v31/v32; W1; neural edit; D5; opening SCORE; installing 512/1536; larger n; lineage; QUAL/EVAL; FULLDEV.R7; rewriting historical locks including MEMORYLIFECYCLEMAP R2/V1; `earned_next`; 0.0.005; instincts; SFNN; another isolated write-formula grid; cue strings in episodes; new η / radius / replay budget / threshold; using bounded match sanity as the stability gate; treating the global σ=0.01 matcher as this package's first failure.
