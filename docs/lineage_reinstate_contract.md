# TM.0.47.REINSTATE contract

**Lab:** TM.0.47.REINSTATE · **Not a v41 candidate.** Product **0.0.004**.

TM046 first-match `generic_reinstatement_fail` stays frozen. Locked oracle telemetry completed **slot i for probe i** with four distinct reinstated addresses, then collapsed to one default action. This wall does **not** tune \(W_k,W_q,W_v\) and does **not** train a new decoder.

## Protocol

Reconstruct the TM046 common checkpoint. Freeze slow `W_act_query`. Teach the same one-shot facts. For each action compare:

| Boundary | What is scored |
| --- | --- |
| `development_reference` | known development P1 that passed the motor precondition |
| `credit_full_rho` | cortical state at one-shot credit |
| `stored_value_direct` | value written to S, scored with recall off |
| `post_reinstatement` | scoring address after normal memory injection |
| `canonical_path` | final organism `event_memory_scores` behavior |

Record hashes, norms, cosine/L2, predicted action, margin, and pre/post scores. Confirm the retrieved episode handle and P1 hash match the intended fact.

## Target

The first failing boundary. If exact replay of a stored/credit state passes and no-memory fails, reinstatement injection is an earned repair target. Otherwise the loss is earlier (credit state or value formation).

## Refuse

K/Q/V edits, new decoder training, TM046 rerun, v41 lock, product-earn.
