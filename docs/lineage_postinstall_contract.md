# TM.0.42.POSTINSTALL contract

**Lab:** TM.0.42.POSTINSTALL · **Not a v40 candidate.**

R2 first-match `canonical_r2_later_learning_not_exercised` remains the lineage stop. This wall is experimental coverage of post-install continuity, not another neural mechanism. No neural, SOCP, recall, fallback-trigger, or half-spacing edits.

## Natural handoff

On preregistered fresh `TM042.POSTINSTALL.*` worlds, teach with `fallback_joint`. The first genuine atomic install of \(W^*\) is checkpointed immediately. Teaching then continues on that same organism. If no install occurs, the cell is `postinstall_not_exercised`.

## Mechanistic handoff

Reconstruct the TM039 diagnostic geometry (`TM039.JOINTSOCP.DEV.`, registry seeds `reg2`/`reg3`) and apply one v37-then-fallback atomic install. This is labeled **mechanistic**, not untouched generalization. Then new credit, reversal, and REST.

## Post-install gates

1. Previously learned mappings remain correct
2. A new mapping is acquired
3. Reversal replaces the old constraint
4. Subsequent fallback, if it runs, remains atomic
5. Novelty/familiarity behavior is unchanged
6. Solver handles never enter ACT routing

## Handoff record

`pre_install_w_hash` → solver status/objective/movement → `installed_w_hash` → later credit → `post_credit_w_hash` and old/new behavior.

## Candidate discussion

Requires both: fallback naturally exercised on untouched worlds, and later learning succeeding after that installation. This lab does not write a candidate lock. Canonical probes only.
