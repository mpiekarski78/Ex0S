# CORTEX isolated D4.R1 statistical contract

**Lab:** TM.0.23.CORTEX.D4.R1.STAT  
**Product:** 0.0.004 · `earned_next=false` · `ex0s=null`  
**Authorized by:** [`cortex_diagnosis.fulldev_r2.lock`](cortex_diagnosis.fulldev_r2.lock)

Narrow D4 only. Historical `score_d4` unchanged. Do not edit `cortex_develop_scorers.py`. Do not drop C6.

Worlds from a new sealed commitment, domain `TM023.D4.R1.`.

Authorized neural law (implement only after this apparatus is on `origin/main`):

1. Keep v16 equal-evidence / familiarity / conflict / three-symbol laws.
2. **S-write recency:** when `CortexMemory.write` commits a record, copy that record's content into `retrieval_buffer[0]` so a just-written fact is immediately recallable.

No capability-specific functions. Gate clear ≥13/16 pairs (main and twin D0+D4). Re-earn C4/C5/C6 before reveal.
