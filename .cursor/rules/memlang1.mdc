---
description: MEMLANG-1 is an autonomous program, not a TM063 diagnostic.
alwaysApply: true
---

# MEMLANG-1

Do not open TM063 as a single diagnostic wall. TM062 closed the transport branch. Representation search happens in MEMLANG-1.

Internal failures are telemetry under `runs/memlang1/`, not `docs/lineage_*.decision.lock` releases.

Do not install W*. Do not retune `EPISODE_MATCH_L2` (0.05). Do not edit `three_memory/neural_cortex.py`, `opaque_memory.py`, or `joint_socp.py` in the freeze. Do not redesign K/Q/V until Stage C unlocks. Never write `cortex.candidate.v41.lock`. Live pointer stays v30. Product remains 0.0.004.

Do not reuse `MEMPROJ_LEARNED` as a Stage A family. The runner must never construct stored values or copy an answer handle into S.

Stages B–E stay locked until the previous integrated gate is green. Only a selected mechanism may receive a frozen candidate commit and one untouched confirmation battery.
